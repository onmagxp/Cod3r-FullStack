const raio = 5.6
const area = Math.PI * Math.pow(raio, 2)
//Math é um object que têm uma constant que é ´PI` e um function que é pow.
//´Math.PI` é do tipo number.
//´Math.pow` é do tipo function.

console.log(area.toFixed(2))
console.log(typeof area) //type: number
console.log(typeof Math) //type: object
console.log(typeof Math.PI) //type: number
console.log(typeof Math.pow)// type: function


